// src/app/models/user.model.ts

export interface User {
    userId: number; // Match with the actual property name in your backend
    name: string; // Adjust to your User model's properties
    email: string; // Example property, replace with actual
    // Add other properties based on the backend response
  }
  